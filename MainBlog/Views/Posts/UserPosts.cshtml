@model MainBlog.ViewModels.UserPostsViewModel
﻿﻿@using Microsoft.AspNetCore.Identity;
@using MainBlog.Models;
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager
@{
    ViewData["Title"] = "Ваш Блог";
    // Layout = null;
    string registeredUsername = Context.Request.Cookies["RegisteredUsername"];
    
}
@* @if (SignInManager.IsSignedIn(User))
{
}
else
{
    <form method="post" asp-controller="AuthReg" asp-action="Logout">
        <input class="enter_submit" type="submit" value="Выход" />
    </form>

} *@
    <H1>Страница пользователя @registeredUsername</H1>

    <div style="width: 100%; height: 100%; background-color: darkred">
        <div id="PostField" style="background-color: steelblue">
            <form method="post" asp-controller="Posts" asp-action="AddUserPosts">
                @Html.TextAreaFor(m => m.Name, new { @class = "userPostsTextArea" })
                @Html.TextAreaFor(m => m.Text, new { @class = "userPostsTextArea" })
                <button type="submit">Опубликовать</button>
            </form>

        </div>
        <div id="allUserPosts">
        </div>
    </div>
    <table border="1" style="border-color: steelblue; border-block-color:antiquewhite;" cellpadding="5" width="100%">

        <thead>
            <tr>
                <td bgcolor="steelblue" colspan="2" style="color: bisque">Все Ваши посты</td>
            </tr>
            <tr>
                <th>
                    @*                 @foreach (var item in Model.Tegs)
                {
                @Html.DisplayNameFor(model => model.Tegs.FirstOrDefault())
                } *@
                    здесь будут теги
                </th>
            </tr>
        </thead>

        <tbody>
            <tr>
                <td>
                    <div>
                        <form style="display:grid">
                            <output>здесь будет время публикации</output>
                            <output>здесь будет сам пост</output>
                            <input type="submit" value="Оставить комментарий" />
                        </form>
                    </div>
                </td>
            </tr>
            <tr>
                <td>
                    asdasdasd
                </td>
            </tr>
            @*         @foreach (var item in Model)
        {
        <tr>
        <td>
        @Html.DisplayFor(model => item.PublicationDate)
        </td>
        <td>
        @Html.DisplayFor(model => item.Text)
        </td>
        <td>
        <button asp-controller="Account" asp-action="EditUser">Сохранить</button>
        </td>
        </tr>
        } *@
        </tbody>
    </table>


